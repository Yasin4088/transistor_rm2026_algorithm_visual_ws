# 同济 YOLOv5 模型接入注解（黑箱调试手册）

> 本文件记录"同济 yolov5"模型在我们流水线中的接入事实与调试要点。
> 该模型对我们属于**黑箱**：只有 OpenVINO IR 文件和行为观察，没有训练源码、数据集或类别真值。

## 1. 模型是什么

| 项目 | 值 |
|---|---|
| 文件 | `src/shared_files/models/yolov5_tongji.xml` / `.bin`（约 2.8MB） |
| 形态 | OpenVINO IR（**不是 onnx**，config 直接指向 `.xml`） |
| 输入 | `images [1,3,640,640]`（u8 BGR，经 ppp 转 f32 RGB 并 /255） |
| 输出 | `output [1,25200,22]`（**anchors-first，与旧 0526 模型同布局**） |
| 来源 | 同济导出；无训练侧资料 |

## 2. 与 RP24 系模型的差异（代码事实）

| 差异点 | 同济 yolov5 | RP24（0526 / FasterNet） |
|---|---|---|
| 预处理 | **letterbox**（等比缩放 + 补零，`RP24_YOLO_letterbox : true`） | 直接拉伸 |
| 类别 8 | **"非装甲板"，postprocess 强制过滤** | 正常类别 |
| 颜色约定 | 0=蓝，1=红 | 0526 的 Object 注释写 blue:1/red:0（与过滤逻辑可能有出入，需实测） |
| 类别顺序 | 未验证（黑箱，最大风险点） | 已知 |
| 模型路径 | 直接给 `.xml`（wrapper 支持跳过 onnx 转换） | 给 `.onnx` |
| 解码分支 | legacy（`[1,25200,22]` anchors-first） | 0526 走 legacy，FasterNet 走 channels-first |

## 3. 解码链路与配置项

**解码自动分派**（`OpenvinoInfer.cpp`）：
- `output_shape[1] < output_shape[2]` → channels-first（FasterNet，`[1,29,N]`）
- 否则 → anchors-first（0526 / 同济，`[1,25200,22]`）
- 同济输出 `[1,25200,22]` → 走 **legacy 分支**，列含义：
  - 0-7：4 个关键点 (x,y)
  - 8：置信度（sigmoid 后）
  - 9-12：颜色独热（None/Purple 丢弃）
  - 13-21：9 类得分

**类别映射**（`RP24_YOLO_Wrapper.h`）：同济沿用 `class_map_legacy[9] = {5,0,1,2,3,4,6,7,7}`——**这是从 0526 复制来的假设，同济类别顺序未验证**。

**配置项**（`config.yaml`）：
```yaml
RP24_YOLO_model_relative_path : "src/shared_files/models/yolov5_tongji.xml"
RP24_YOLO_model_family : "tongji_yolov5"   # rp24 或 tongji_yolov5
RP24_YOLO_letterbox : true                 # 同济必须开
RP24_YOLO_input_size : 640
RP24_YOLO_infer_threads : 8
RP24_YOLO_infer_streams : 1
```

**同济特有代码**（`RP24_YOLO_Wrapper.cpp`）：
- `tongji_model_` 为 true 时，postprocess 跳过 `label == 8`（非装甲板）
- `letterbox_` 为 true 时，预处理等比缩放置左上角补零；后处理按同一 scale 映射回原图

## 4. 调试要点（黑箱验证清单）

1. **确认加载正确**：启动日志应有
   ```
   [INFO] RP24_YOLO model path: .../yolov5_tongji.xml
   [INFO] IR files already exist, loading: .../yolov5_tongji.xml
   ```
   若出现 `IR model files not found` → xml/bin 缺失或路径错。

2. **颜色是否反**：同济标注 0=蓝 1=红，而 legacy 解码的过滤逻辑是"敌方蓝(0)跳过 1、敌方红(1)跳过 0"，两者一致才正常。若画面里**敌我颜色检测反了**，说明模型实际颜色通道与标注不符，需调整 legacy 解码里的颜色过滤。

3. **灯条/关键点顺序**：若灯条斜、左右灯条串点、装甲板形状怪异 → 关键点顺序与 `[左灯条上, 左灯条下, 右灯条上, 右灯条下]` 假设不符，调整 `OpenvinoInfer.cpp` legacy 分支的取点下标。

4. **类别映射（最高风险）**：`class_map_legacy` 沿用 0526 的类别顺序假设。若检测正常但**数字识别错乱**（比如把 3 识别成 2），说明同济 9 类顺序不同，需要对同济模型**重新标定 class_map**。标定方法：贴不同数字装甲板图看输出 label。

5. **letterbox 是否匹配**：若检测框**位置整体偏移**（尤其图像边缘目标），检查 letterbox 的 scale 与后处理映射是否一致（当前实现：等比 scale 置左上角、坐标统一 /scale）。

6. **类别 8 过滤**：若画面仍有"非装甲板"误报，确认 `tongji_model_` 开关生效（config `model_family` 为 `tongji_yolov5`）。

7. **性能**：每 90 帧报告看 `yolo_infer`（纯推理）与 `yolo_infer_wait`（排队）；benchmark：
   ```bash
   benchmark_app -m src/shared_files/models/yolov5_tongji.xml -d CPU -api sync -niter 200 -nthreads 8 -hint none
   ```

## 5. 备用切换

换回 RP24 0526 INT8（config 里已有注释好的备用块）：
```yaml
# 注释同济三行：
#RP24_YOLO_model_relative_path : "src/shared_files/models/yolov5_tongji.xml"
#RP24_YOLO_model_family : "tongji_yolov5"
#RP24_YOLO_letterbox : true
# 取消备用三行注释：
RP24_YOLO_model_relative_path : "src/shared_files/models/RP24_YOLO/0526_int8.onnx"
RP24_YOLO_model_family : "rp24"
RP24_YOLO_letterbox : false
```
纯配置切换，无需重编译（双格式解码自动识别）。

## 6. 已知风险 / 未验证项

- **同济 9 类类别顺序未验证**（class_map_legacy 是 0526 的假设）——数字识别错乱时优先查这里；
- 颜色通道实际含义未验证（依赖标注 0=蓝 1=红，可能相反）；
- 无训练侧信息：输入归一化、letterbox 边距、类别真值都只能靠行为反推；
- 阈值（conf 0.65 / nms 0.45）沿用 RP24 默认，同济模型可能需要单独调。
